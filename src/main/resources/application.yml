#spring.application.name=securitytest
spring:
  application:
    name: securitytest
  datasource:
    url: jdbc:postgresql://localhost:5432/securitytest
    username: postgres
    password: saimun
    driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        ddl-auto: update  # options: validate, update, create, create-drop
      show-sql: true  # to log the SQL queries
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    user:
      name: saimun
      password: password
server:
  port: 8081